on:
  push:
    branches:
    - test

jobs:
  test_and_merge:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2.5.0
    - uses: actions/setup-node@v3.5.1
      with:
        node-version: 18.x
        cache: npm
    - run: npm i
    - run: npm run test
    - run: npm run merge
